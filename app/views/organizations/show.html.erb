
<html>
    <h1>WorldChicago partner organizations</h1>
    <table>
        <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Description</th>
        </tr>

            <tr>
                <td><%= @organization.name %></td>
                <td><%= @organization.address %></td>
                <td><%= @organization.description %></td>
            </tr>
    </table>

    <br>
    <table>
        <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Description</th>
        </tr>

        <% @contacts.each do |contact| %>
            <tr>
                <td><%= contact.name %></td>
                <td><%= contact.phone_number %></td>
                <td><%= contact.email %></td>
            </tr>
        <% end %>
    </table>
</html>
